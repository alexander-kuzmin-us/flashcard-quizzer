# Runtime: the application itself uses only the Python standard library
# (Python 3.10+). Everything below is development / QA tooling.

# Testing
pytest>=7.0.0
pytest-cov>=4.0.0

# Formatting and linting
black>=23.0.0
isort>=5.12.0
flake8>=6.0.0

# Static type checking
mypy>=1.5.0

# Security linting
bandit>=1.7.0
